<%= form_for(article) do |f| %>
  <%= f.label :title, 'Enter a title' %>
  <%= f.text_field :title %>

  <%= f.label :content, 'Enter content' %>
  <%= f.text_field :content %>

  <%= f.submit %>
<% end %>
